# Changelog — Bliss Tactile Symbols .scad (symbol designer file)

User-visible changes to the symbol designer `.scad` file, newest first. This is a
SEPARATE version axis from the web app (see CHANGELOG.md for the app). The bullets
here are shown verbatim in the in-app "Symbol designer update available" dialog, so
write them the way a user reads them.

Release flow: bump `scad_version` in `Bliss Tactile Symbols.scad`, rename the
`## Unreleased` heading below to `## Version N` (matching), then run
`node scripts/publish-scad-version.mjs`. See RELEASING.md.

## Unreleased (next version)

- _Nothing yet._

## Version 1

- First published version of the symbol designer file.
