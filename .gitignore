# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*~

# Node (only dev scripts use node; no deps are committed)
node_modules/

# Local scratch
*.log

# Claude Code machine-local settings (incl. launch.json — the local preview
# server config, which points at this machine's python)
.claude/
